# 咩咩坂46 UX/UI 调整文档（已确认范围版）

> 文档目的：基于已完成的 UX/UI 评审，收敛出一份“只包含当前确认要做内容”的执行文档，供后续任务直接评估实现成本、识别风险并开始实施。
>
> 目标受众：后续负责实施、拆分、估时和回归验证的智能体或开发者。
>
> 项目根目录：`C:\Users\Windows11\WorkBuddy\hitsujizaka46\`
>
> 版本状态：`已按当前取舍收敛`
>
> 更新时间：`2026-08-29`

---

## 1. 使用说明

- 本文档只保留“当前确认要做”的调整项。
- 之前评审中提到、但当前明确“不做”的建议，已不再作为路线图的一部分推进。
- 为避免后续任务误判范围，本文档单独列出“明确排除项”，实现时不要顺手把这些内容带入。
- 如后续决策变化，应在本文档基础上增删，不要回退到更宽泛的评审版本。

---

## 2. 本轮确认要做的范围

本轮确认保留的调整项如下：

- 手机端 `stage` / `special` 详情页改单列
- 首页首屏与全局 reveal 动画降依赖
- 固定控件层级统一降噪
- 首页卡片区文案去桌面化
- 全站响应式策略统一
- 滚动提示仅在真正需要时出现
- 统一按钮与交互尺寸规范
- 完整补齐交互状态系统
- 提升次级文字与浅色边框对比度
- 统一品牌与功能文案语气
- 统一悬浮控件的视觉语言
- 统一首页卡片与详情页卡片的视觉层级
- 建立跨断点验收截图与回归基线
- 新增：`制服与太阳` 曲目列表支持点击整行直接播放，不必只点播放入口

---

## 3. 明确不做的范围

以下方向本轮明确不做，后续实现时应视为排除范围：

- 不重做首页首屏导览结构，不新增主 CTA 或次级入口
- 不重构首页整体信息架构，不新增“精选推荐”“时间线”“纪念节点”等新模块
- 不重构 `制服与太阳` 的内容组织，不做标签、推荐理由、分组或“展开全部”
- 不重做 `公演舞台` / `特殊舞台` 首页卡片的信息层，不新增曲目数、更新时间、摘要信息
- 不给 `stage` / `special` 详情页新增筛选、排序、年份分组、URL 状态管理等产品化能力
- 不重构 `SongCard` 展开区的信息结构，只允许为了小屏可读性做必要适配
- 不重做移动端顶栏结构，不重构移动端搜索入口策略，不单独重做播放器交互模型
- 不新增默认推荐排序、精选配置、标签配置或额外数据层
- 不新增首页级内容策展能力

说明：

- 如果某个已确认任务的实现过程中，出现会顺带引入以上排除项的情况，应优先回避。
- 若确实不可避免，必须先重新评估范围，而不是默认扩张需求。

---

## 4. 评审依据保留

### 4.1 实际查看页面

- 首页：`/`
- 公演舞台详情：`/stage/0/`
- 特殊舞台详情：`/special/AKB48/`

### 4.2 实际查看断点

- 桌面端主视口约：`1233 x 1229`
- 平板截图视口：`768 x 1024`
- 手机截图视口：`390 x 844`

### 4.3 本地证据文件

- 首页手机截图：`web/_uxshots/home-mobile-wait.png`
- 首页平板截图：`web/_uxshots/home-tablet-wait.png`
- 公演详情手机展开截图：`web/_uxshots/stage-mobile-open.png`
- 特殊舞台详情手机展开截图：`web/_uxshots/special-mobile-open.png`

### 4.4 主要涉及代码位置

- 首页首屏：`web/src/components/Masthead.tsx`、`web/src/components/Masthead.css`
- 全局样式：`web/src/app/globals.css`
- 首页公演卡片区：`web/src/components/StageGrid.tsx`、`web/src/components/StageGrid.css`
- 首页特殊舞台卡片区：`web/src/components/SpecialGrid.tsx`、`web/src/components/SpecialGrid.css`
- 公演详情页：`web/src/app/stage/[id]/page.tsx`、`web/src/app/stage/[id]/page.css`
- 特殊舞台详情页：`web/src/app/special/[origin]/page.tsx`、`web/src/app/special/[origin]/page.css`
- 曲目折叠卡片：`web/src/components/SongCard.tsx`、`web/src/components/SongCard.css`
- 首页毕业公演列表：`web/src/components/Setlist.tsx`、`web/src/components/Setlist.css`
- 顶栏响应式：`web/src/components/Topbar.css`
- 滚动提示：`web/src/components/ScrollHint.tsx`、`web/src/components/ScrollHint.css`
- 回顶控件：`web/src/components/ScrollControl.css`
- 移动端浮动搜索：`web/src/components/MobileSearchDock.tsx`、`web/src/components/MobileSearchDock.css`
- 音乐播放器：`web/src/components/BgmPlayer.css`

---

## 5. 调整任务清单（按迫切性排序）

## 5.1 P0 最高优先级

### T1. 手机端 `stage` / `special` 详情页改为单列

**为什么现在最急**

- 当前小屏下 `公演舞台` 和 `特殊舞台` 详情页仍维持两列，曲名截断频繁，展开信息过挤。
- 这已经直接影响手机端核心浏览体验，是当前最明确的可用性问题。

**怎么改**

- 在 `<=520px` 或确认后的手机断点下，把 `stage/[id]` 与 `special/[origin]` 的歌曲网格统一切为单列。
- 保留平板双列，但要重新检查间距、卡片内边距和展开态占位。
- 若展开态当前依赖 `grid-column: 1 / -1`，改单列时要一起验证布局回流是否自然。

**涉及文件**

- `web/src/app/stage/[id]/page.css`
- `web/src/app/special/[origin]/page.css`
- `web/src/components/SongCard.css`

**不应顺带做的事**

- 不在本任务里新增筛选、排序、年份分组。
- 不在本任务里重做详情页结构。

**验收标准**

- 手机端详情页默认一列。
- 曲名截断频率显著下降。
- 展开后的演出记录可读性明显改善。

### T2. 首页首屏与全局 reveal 动画降依赖

**为什么现在最急**

- 当前首屏部分关键信息依赖字体 ready 与 reveal 才完全成立，移动端容易出现“页面加载了，但主视觉还没站起来”的空场感。
- 这会直接影响首屏稳定性和第一印象。

**怎么改**

- 让首屏关键信息默认可见，动画只做增强，不再负责“把内容显现出来”。
- 重新检查 `document.fonts.ready` 的使用范围，避免品牌标题和核心内容完全受其控制。
- 如果全局 reveal 机制默认让元素初始透明，首屏区域要单独豁免或改成轻量进入动画。

**涉及文件**

- `web/src/components/Masthead.tsx`
- `web/src/components/Masthead.css`
- `web/src/app/globals.css`

**不应顺带做的事**

- 不重构首屏信息结构。
- 不新增 CTA、精选入口或导览模块。

**验收标准**

- 首屏关键内容在加载早期即可稳定看到。
- 动画存在，但不是信息出现的前提。

### T3. 固定控件层级统一降噪

**为什么现在最急**

- 搜索、播放器、滚动提示、回顶按钮在小屏下容易同时争抢注意力，破坏内容阅读的宁静感。
- 这是跨首页与详情页都存在的共性问题。

**怎么改**

- 统一 fixed 控件在不同路由、滚动位置和断点下的出现规则。
- 首页首屏允许保留一个强提示控件，其余控件弱化或延后出现。
- 详情页优先保证内容阅读，不要让悬浮控件从进页开始持续抢占角落。

**涉及文件**

- `web/src/components/MobileSearchDock.css`
- `web/src/components/BgmPlayer.css`
- `web/src/components/ScrollHint.tsx`
- `web/src/components/ScrollHint.css`
- `web/src/components/ScrollControl.css`

**不应顺带做的事**

- 不重做搜索入口产品逻辑。
- 不重做播放器功能模型。

**验收标准**

- 小屏下不会同时出现多个高存在感 fixed 控件。
- 主内容优先级高于浮层控件。

### T4. 全站响应式策略统一

**为什么现在最急**

- 当前首页、详情页、列表区各自断点策略不完全一致，导致同站体验割裂。
- 如果不先统一策略，后续视觉和交互打磨会一直反复。

**怎么改**

- 先定义 PC / 平板 / 手机三档的统一原则：
  - 手机优先单列、低密度、强可读性
  - 平板保留双列但增加留白
  - 桌面允许更高信息密度
- 对首页卡片区、`Setlist`、详情页歌曲网格逐一对齐断点策略。

**涉及文件**

- `web/src/app/globals.css`
- `web/src/components/StageGrid.css`
- `web/src/components/SpecialGrid.css`
- `web/src/components/Setlist.css`
- `web/src/app/stage/[id]/page.css`
- `web/src/app/special/[origin]/page.css`
- `web/src/components/Topbar.css`

**不应顺带做的事**

- 不借此重排首页模块顺序。
- 不引入新的页面结构方案。

**验收标准**

- 相同类型组件在相近断点下有一致策略。
- 手机端不再出现明显“桌面缩小版”痕迹。

## 5.2 P1 高优先级

### T5. 首页卡片区文案去桌面化

**为什么要做**

- 当前“悬停预览曲目 · 点击进入详情”类文案只适合桌面，不适合手机。
- 这是低风险但高感知的修正项。

**怎么改**

- 把文案改成不依赖 hover 的中性表达。
- 推荐方向：
  - `浏览曲目与详情`
  - `进入舞台档案`
  - `查看分组与收录曲目`
- 同时检查首页相关提示语，保证移动端语义成立。

**涉及文件**

- `web/src/components/StageGrid.tsx`
- `web/src/components/SpecialGrid.tsx`

**不应顺带做的事**

- 不新增卡片摘要信息。
- 不重构卡片组件。

**验收标准**

- 文案不再暗示 hover 是必要前提。
- 桌面端和移动端对文案理解一致。

### T6. 滚动提示只在真正需要时出现

**为什么要做**

- 滚动提示对首屏有帮助，但若长期存在，会像外挂控件一样打扰内容阅读。

**怎么改**

- 只在首页首屏初始阶段展示。
- 一旦离开首屏或用户已经完成首次滚动，就自动淡出。
- 不要求在详情页或中后段内容持续存在。

**涉及文件**

- `web/src/components/ScrollHint.tsx`
- `web/src/components/ScrollHint.css`
- `web/src/app/globals.css`

**不应顺带做的事**

- 不新增其他形式的首页导览。

**验收标准**

- 滚动提示只承担首屏引导，不长期占位。

### T7. `制服与太阳` 曲目列表支持整行点击直接播放

**为什么要做**

- 当前每一行若必须点播放入口才能播，操作路径偏窄，尤其手机端不够顺手。
- 这是高频内容区，整行热区能直接提升使用效率。

**怎么改**

- 让每一行曲目在非冲突区域支持整行点击触发播放。
- 原有播放按钮可以保留，但不再是唯一触发点。
- 需要处理好事件优先级，避免与其他链接、选中文本、键盘焦点冲突。

**涉及文件**

- `web/src/components/Setlist.tsx`
- `web/src/components/Setlist.css`

**不应顺带做的事**

- 不重构 `制服与太阳` 的列表结构。
- 不新增标签、分组或推荐层。

**验收标准**

- 点击曲目整行即可播放。
- 播放按钮仍可用。
- 不会误触发其他操作。

## 5.3 P2 中高优先级

### T8. 统一按钮与交互尺寸规范

**为什么要做**

- 当前站点已有轻盈审美，但部分点击热区偏小，尤其手机端更明显。
- 这是可访问性与舒适度的基础项。

**怎么改**

- 为按钮、导航项、输入框、交互芯片统一最小高度和最小点击区。
- 优先覆盖高频组件：
  - 顶栏按钮
  - 列表行
  - 搜索入口
  - 曲目卡片操作区

**涉及文件**

- `web/src/app/globals.css`
- `web/src/components/*.css`

**不应顺带做的事**

- 不借机重做所有组件视觉造型。

**验收标准**

- 高频交互元素的点击舒适度明显提高。
- 小屏下不再出现明显过小热区。

### T9. 完整补齐交互状态系统

**为什么要做**

- 当前很多组件主要覆盖默认态和部分 hover 态，状态系统还不够完整。
- 这会让整站看起来不像一套成熟产品，而像若干局部样式集合。

**怎么改**

- 为通用组件明确统一状态：
  - `default`
  - `hover`
  - `active`
  - `focus-visible`
  - `disabled`
- 优先应用到按钮、导航、输入框、卡片、列表行和固定控件。

**涉及文件**

- `web/src/app/globals.css`
- `web/src/components/*.css`

**不应顺带做的事**

- 不引入与现有气质冲突的强烈高饱和反馈。

**验收标准**

- 常用控件状态一致。
- 键盘焦点可见。

### T10. 提升次级文字与浅色边框对比度

**为什么要做**

- 现有浅绿、浅灰系风格有审美优势，但次级信息在部分场景下偏淡，尤其小屏和低亮度环境更容易失真。

**怎么改**

- 微调次级文字颜色和边框颜色，不改变整体气质，只提升可读性下限。
- 优先修正：
  - 次级说明文字
  - 卡片描边
  - 输入框边界
  - 浮层轮廓

**涉及文件**

- `web/src/app/globals.css`
- `web/src/components/*.css`

**不应顺带做的事**

- 不把整体色系改得过重或过商业化。

**验收标准**

- 弱信息仍清晰可辨。
- 风格仍保持克制轻盈。

### T11. 统一品牌与功能文案语气

**为什么要做**

- 当前部分文案偏操作说明式，不够像统一的品牌系统。
- 在不改结构的前提下，文案统一是提升完成度的高性价比手段。

**怎么改**

- 建立三层口径：
  - 品牌层：温和、纪念、克制
  - 功能层：清晰、简洁、不说废话
  - 法务层：正式、可信
- 先覆盖首页提示文案、卡片提示语、按钮文本和页脚说明。

**涉及文件**

- `web/src/components/*.tsx`
- `web/src/app/**/*.tsx`

**不应顺带做的事**

- 不新增首页叙事模块。
- 不用文案替代本该通过结构解决的问题。

**验收标准**

- 文案风格统一，不再出现明显“功能提示堆叠感”。

### T12. 统一悬浮控件的视觉语言

**为什么要做**

- 即使控件层级已经降噪，如果视觉语言不统一，它们仍会像几套独立外挂拼在页面上。

**怎么改**

- 统一悬浮控件的：
  - 圆角
  - 边框
  - 透明度
  - 模糊
  - 阴影
  - 浮现/淡出方式
- 包括搜索入口、播放器、回顶按钮、滚动提示。

**涉及文件**

- `web/src/components/MobileSearchDock.css`
- `web/src/components/BgmPlayer.css`
- `web/src/components/ScrollHint.css`
- `web/src/components/ScrollControl.css`

**不应顺带做的事**

- 不新增新的悬浮控件种类。

**验收标准**

- 所有悬浮控件像同一套设计系统的一部分。

### T13. 统一首页卡片与详情页卡片的视觉层级

**为什么要做**

- 首页卡片与详情页 `SongCard` 目前像不同代际组件，整体完成度不够一致。

**怎么改**

- 对齐卡片系统的基础语言：
  - 标题层级
  - 边框与阴影
  - 默认背景
  - 圆角与留白
  - 状态切换反馈
- 注意这里只做视觉层级统一，不做信息结构重构。

**涉及文件**

- `web/src/components/StageCard.css`
- `web/src/components/SpecialGrid.css`
- `web/src/components/SongCard.css`
- `web/src/app/globals.css`

**不应顺带做的事**

- 不给首页卡片新增摘要信息。
- 不重做详情页卡片的内容结构。

**验收标准**

- 首页卡片与详情页卡片看起来属于同一套产品语言。

## 5.4 P3 工程保障优先级

### T14. 建立跨断点验收截图与回归基线

**为什么要做**

- 这轮调整以响应式、控件层级和通用样式为主，如果没有回归基线，很容易改一处坏一处。

**怎么改**

- 为以下页面建立固定断点截图与最小回归清单：
  - 首页
  - `stage/0`
  - `special/AKB48`
- 至少覆盖：
  - `390px`
  - `768px`
  - `1233px`
- 回归点包含：
  - 首屏初始可见状态
  - 详情页单列表现
  - fixed 控件显示状态
  - 列表行点击播放

**涉及文件**

- `web/_uxshots/*`
- 相关测试或记录脚本（如后续补充）

**不应顺带做的事**

- 不把它扩展成完整自动化测试建设项目。

**验收标准**

- 后续任务可以用固定截图和清单快速判断是否回归。

---

## 6. 推荐实施顺序

建议按以下顺序推进，避免相互打架：

1. `T1` 手机端详情页改单列
2. `T2` 首屏与 reveal 降依赖
3. `T4` 统一响应式策略
4. `T3` 固定控件层级统一降噪
5. `T6` 滚动提示出现时机收敛
6. `T7` `制服与太阳` 整行点击播放
7. `T8` 按钮与交互尺寸规范
8. `T9` 交互状态系统
9. `T10` 次级文字与边框对比度
10. `T5` 首页卡片区文案去桌面化
11. `T11` 文案语气统一
12. `T12` 悬浮控件视觉语言统一
13. `T13` 卡片系统视觉层级统一
14. `T14` 建立回归基线并验收

说明：

- `T4` 虽然是系统项，但不建议拖到最后，否则前面修的断点逻辑可能要返工。
- `T14` 虽然放在最后列出，但实施中建议每完成一大类改动就同步补截图。

---

## 7. 成本评估提示

### 7.1 低成本

- `T5` 首页卡片区文案去桌面化
- `T6` 滚动提示只在真正需要时出现
- `T10` 提升次级文字与浅色边框对比度

### 7.2 中成本

- `T1` 手机端详情页改单列
- `T2` 首屏与 reveal 动画降依赖
- `T3` 固定控件层级统一降噪
- `T7` `制服与太阳` 整行点击播放
- `T8` 按钮与交互尺寸规范
- `T11` 文案语气统一
- `T12` 悬浮控件视觉语言统一
- `T13` 卡片视觉层级统一

### 7.3 中高成本

- `T4` 全站响应式策略统一
- `T9` 完整补齐交互状态系统
- `T14` 建立跨断点验收截图与回归基线

---

## 8. 后续任务执行边界

- 可以调整样式、断点、可见性规则、状态系统、文案和热区行为。
- 可以为了单列化和整行点击播放，做必要的轻微结构调整。
- 不要在未重新确认的前提下新增首页模块、详情页筛选、排序、分组、推荐和额外数据配置。
- 不要把当前范围偷渡成“顺便做一个更完整的产品化改版”。

---

## 9. 最终结论

- 本轮不是做“大改版”，而是做一轮高价值的跨端体验与设计系统收敛。
- 核心目标不是重画结构，而是先把当前已存在的页面做得更稳、更顺手、更统一。
- 后续实现任务应严格围绕以下四类目标展开：
  - 小屏可读性修复
  - 首屏与动效稳定性修复
  - fixed 控件与响应式系统收敛
  - 通用交互、文案与视觉语言统一

- 只要这轮执行到位，站点虽然不改变整体内容结构，但完成度会明显提升，也更适合作为下一轮是否大改版的判断基础。
